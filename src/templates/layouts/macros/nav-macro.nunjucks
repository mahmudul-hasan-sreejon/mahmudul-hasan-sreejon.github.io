{% macro active(activePage='home') %}
  <nav>
    <a href="#" class="{%if activePage == 'home' %} active {% endif %}">Home</a>
    <a href="#" class="{%if activePage == 'home' %} active {% endif %}">About</a>
    <a href="#" class="{%if activePage == 'home' %} active {% endif %}">Contact</a>
  </nav>
{% endmacro %}
