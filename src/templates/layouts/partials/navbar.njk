    <!-- navbar start -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top" id="sideNav">
      <!-- navbar title start -->
      <a class="navbar-brand js-scroll-trigger" href="#page-top">
        <span class="d-block d-lg-none">{{personalInfo.name.first}} {{personalInfo.name.last}} {{personalInfo.name.pet}}</span>
        <span class="d-none d-lg-block">
          <img class="img-fluid img-profile rounded-circle mx-auto mb-2" src="{{navbar.profileImage.src}}" alt="{{navbar.profileImage.alt}}" title="{{navbar.profileImage.title}}">
        </span>
      </a>
      <!-- navbar title end -->
      
      <!-- navbar button start -->
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <!-- navbar button end -->

      <!-- navbar menu start -->
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav">
          {% for item in navbar.menuItem %}
          <li class="nav-item">
            <a class="nav-link js-scroll-trigger" href="{{item.href}}">{{item.name}}</a>
          </li>
          {% endfor %}
        </ul>
      </div>
      <!-- navbar menu end -->
    </nav>
    <!-- navbar end -->